# <機能名> — 技術設計

対応 spec: [spec.md](spec.md)

## 方針概要

<設計の要点を 3〜5 文で。検討した代替案があれば「採らなかった案と理由」を 1 行ずつ>

## 影響レイヤー

レイヤー方向(`model` ← `layout` ← `store`/`components`)を守ること。

| レイヤー | 変更内容 | 新規/変更ファイル |
|---|---|---|
| model | <純 TS ロジック。理論計算・型> | |
| layout | <ジオメトリ計算> | |
| store | <状態・永続化> | |
| components | <View。薄く保つ> | |
| audio | <再生関連(あれば)> | |

## データモデル変更

- **保存形式の変更**: あり / なし
- ありの場合: `schemaVersion` を <N> に上げ、`migrate.ts` に移行関数を追加する。旧データの扱い: <デフォルト値・変換規則>
- JSON エクスポート/インポートへの影響: <あれば>

## 不変条件チェックリスト(CLAUDE.md)

設計がこれらを壊さないことを確認する。触れる場合は対処を書く:

- [ ] 音高を保存しない(弦+フレットから `pitchFromTab` で導出)
- [ ] リズム計算は `fraction.ts` の分数演算(浮動小数点禁止)
- [ ] 永続化データは `migrate.ts` を通る
- [ ] 拍子は `effectiveTimeSignature()` 経由で読む
- [ ] undo 粒度 = store 更新 1 回
- [ ] 弦番号規約(TAB: index 0 = 1弦 / ChordBlock.frets: index 0 = 6弦)
- [ ] キーバインド変更時は `keymap.ts` と `TabBlockView.tsx` の両方を同期

## UI 設計

<画面・操作の変更。キーバインド追加があれば keymap.ts の表を先に更新する旨。UI テキストは日本語>

## テスト戦略

- **model / layout**(テストの主戦場): <純関数として何をテストするか。境界値・分数の厳密性>
- **手動確認**: <`npm run dev` で確認する操作手順>

## 受け入れ基準との対応

| AC | 検証方法(テスト名 or 手動手順) |
|---|---|
| AC-1 | |
| AC-2 | |
